#ifndef HEALTH__H
#define HEALTH__H
#include<string>
#include<SDL.h>
#include "ioManager.h"

class Health {
public:
  Health();
  void drawHud(SDL_Surface * const&, const IOManager&) const;
};
#endif
